# Study with AI — 작업 규칙

## 프로젝트 목적

이 저장소는 개발을 처음 배우는 사용자가 대화를 통해 작은 웹페이지를 하나씩 만들며 학습하는 연습 공간이다.

## 학습자 수준과 목표

- Python 경험은 충분하지만 HTML과 JavaScript는 처음이다.
- 우선순위는 HTML/CSS 기초, JavaScript 상호작용, 작은 웹앱 완성 경험이다.
- 설명은 장황하게 하지 말고, 작업에 필요한 핵심만 한국어로 설명한다.

## 작업 방식

- 파일을 만들거나 수정하기 전에 변경 목적과 범위를 짧게 제안하고, 사용자의 승인을 받은 뒤 수정한다.
- 사용자가 이미 구체적으로 승인한 변경만 수행한다.
- 새로운 기능이나 어려운 개념이 나오면 짧고 쉬운 말로 설명한다.
- 라이브러리나 프레임워크는 기본적으로 사용하지 않는다. HTML, CSS, 순수 JavaScript부터 사용하며, 라이브러리가 도움이 되는 경우에는 이유와 선택지를 먼저 설명하고 승인을 받는다.

## 파일 구성과 확인

- 각 연습 웹페이지는 `web/` 아래에 독립된 하위 폴더로 만든다.
- 하위 폴더 이름은 기존 이름과 겹치지 않게 번호와 목적을 사용한다.
  - 예: `web/01-intro-page/`, `web/02-button-practice/`
- 기존 연습 페이지나 사용자의 파일을 덮어쓰지 않는다.
- 변경 후 기본 동작을 확인하고, 사용자가 브라우저에서 확인할 간단한 항목을 함께 안내한다.

## Git 저장 규칙

- 내가 수정한 코드나 웹페이지 파일은 작업 완료 후, 관련 파일만 스테이징해 설명이 담긴 커밋을 만든다.
- 커밋 뒤에는 현재 브랜치의 `origin` 원격 저장소로 푸시한다.
- 사용자가 커밋 또는 푸시를 하지 말라고 명시한 경우에는 이 규칙을 적용하지 않는다.
- 사용자가 이미 수정해 둔 관련 없는 파일은 스테이징하거나 커밋하지 않는다.
